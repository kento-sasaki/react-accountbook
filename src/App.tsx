import React, { FC } from "react";
import { LoginForm } from "./components/loginForm";

const App: FC = () => {
  return (
    <div className="App">
      <LoginForm />
    </div>
  );
};

export default App;
